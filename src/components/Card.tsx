// import { useState } from 'react';
// import { Flashcard, CardProps } from "../types";

// const Card: React.FC<CardProps> = ({ card, side }) => {
//     const [faceUp, setFaceUp] = useState<boolean>(side);

//     const flip = () => {
//         setFaceUp(!faceUp);
//     };
    
//     return (
//         // eventually, try to add a card flip animation
//         <div className="card" onClick={() => flip()}>
//             <p>{side ? card.front : card.back }</p>
//         </div>
//     );
// };

// export default Card;

export {};